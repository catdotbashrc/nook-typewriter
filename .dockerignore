# ==================================================================
# Nook Typewriter - Docker Build Context Ignore
# ==================================================================
# This file optimizes Docker builds by excluding unnecessary files
# from the build context, reducing build time and image size

# ------------------------------------------------------------------
# Version Control
# ------------------------------------------------------------------
.git/
.gitignore
.gitattributes
.gitmodules

# ------------------------------------------------------------------
# Documentation (not needed in Docker images)
# ------------------------------------------------------------------
*.md
!CLAUDE.md  # Keep AI guidance for reference
docs/
design/
images/
*.pdf
*.txt

# ------------------------------------------------------------------
# Docker & CI/CD
# ------------------------------------------------------------------
docker-compose.yml
docker-compose.*.yml
.dockerignore
Dockerfile*
*.dockerfile
# Exception: We might need our own Dockerfiles for reference
!build/docker/*.dockerfile

# ------------------------------------------------------------------
# IDE and Editor Files
# ------------------------------------------------------------------
.vscode/
.idea/
.cursor/
*.swp
*.swo
*.swn
*~
.*.swp
\#*\#
.\#*

# ------------------------------------------------------------------
# OS Generated Files
# ------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.directory
.Trash-*

# ------------------------------------------------------------------
# Build Artifacts & Outputs
# ------------------------------------------------------------------
firmware/
releases/
build/output/
deployment_package/
cwm_package/
*.img
*.zip
*.iso
*.bin
*.elf
# Exclude all tar.gz except lenny-rootfs.tar.gz needed for production build
jesteros-*.tar.gz
nook-mvp-rootfs*.tar.gz
docker-backup*.tar.gz
sdcard-backup*.tar.gz

# ------------------------------------------------------------------
# Kernel Build Artifacts (Large - 467MB!)
# ------------------------------------------------------------------
# Exclude entire kernel source directory - not needed for Docker builds
source/kernel/
# Only exception: keep essential config files if needed
!source/kernel/config/

# ------------------------------------------------------------------
# Test Artifacts
# ------------------------------------------------------------------
tests/reports/
tests/**/*.log
test_report_*.md
coverage/
*.coverage
*.gcov

# ------------------------------------------------------------------
# Logs & Temporary Files
# ------------------------------------------------------------------
*.log
logs/
*.err
*.out
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup
*.old
*.orig

# ------------------------------------------------------------------
# Large Files Not Needed for Build
# ------------------------------------------------------------------
images/
activate/
backups/
wifi/
android-ndk-*/
ndk/
toolchain/
sdcard/
*.sdcard
# Keep lenny-rootfs.tar.gz for production build
# lenny-rootfs.tar.gz is needed for authentic testing
lenny-rootfs/

# ------------------------------------------------------------------
# Personal/Local Files
# ------------------------------------------------------------------
notes/
personal/
TODO.personal
.zk/
**/.zk/

# ------------------------------------------------------------------
# Python (helper scripts)
# ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
dist/
.pytest_cache/

# ------------------------------------------------------------------
# EXPLICITLY INCLUDE for Docker builds
# ------------------------------------------------------------------
# Source code and scripts
!source/
!source/**/*.sh
!source/**/*.c
!source/**/*.h
!source/configs/
!source/configs/**

# Build scripts
!build/scripts/
!build/scripts/**

# Essential configurations
!Makefile
!build.conf
!project.conf
!VERSION

# Boot configurations
!boot/
!boot/**
