# JesterOS Court Jester Service Configuration
# The heart and soul of the writing companion

SERVICE_NAME="Court Jester"
SERVICE_DESC="ASCII art mood daemon and writing companion"
SERVICE_EXEC="/usr/local/bin/jester-daemon.sh"
SERVICE_PIDFILE="/var/run/jesteros/jester.pid"
SERVICE_ARGS="--daemon"
SERVICE_DEPS=""  # No dependencies - the jester leads!
SERVICE_START_MSG="🎭 The Jester awakens with bells jingling!"
SERVICE_STOP_MSG="🎭 The Jester retires to chambers..."
SERVICE_HEALTH_CHECK="test -f /var/jesteros/jester && test -f /var/lib/jester/state"

# Memory limit (in KB) - Keep the jester lean!
SERVICE_MEM_LIMIT=512

# Auto-restart on failure
SERVICE_RESTART=true
SERVICE_RESTART_DELAY=5